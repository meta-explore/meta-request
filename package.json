{
    "name": "meta-request",
    "version": "0.0.1",
    "private": false,
    "description": "meta-request",
    "main": "lib/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "compile": "rimraf lib/* && babel src -d lib --no-babelrc --presets=es2015,stage-0",
        "prepublish": "npm run compile"
    },
    "author": "meta-explore",
    "license": "ISC",
    "dependencies": {
        "js-cookie": "^2.2.0",
        "query-string": "^6.4.0"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-es2015": "^6.24.1",
        "babel-preset-stage-0": "^6.24.1",
        "rimraf": "^3.0.0"
    }
}
